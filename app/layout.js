export const metadata = { title: 'OrbitFS', description: 'Cloud workspace platform' };

export default function RootLayout({ children }) {
  return <html lang="en"><body style={{margin:0,background:'#0b1020',color:'#eef2ff',fontFamily:'system-ui,-apple-system,sans-serif'}}>{children}</body></html>;
}

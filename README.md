# OrbitFS Workspace Base V2

Fresh Workspaces-first panel build.

- `Workspaces` is the primary shell.
- `Workspace Explorer` lives under `/workspaces/explorer`.
- Add-ons are intentionally deferred for a later phase.
